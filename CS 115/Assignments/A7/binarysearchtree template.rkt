;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-beginner-abbr-reader.ss" "lang")((modname |binarysearchtree template|) (read-case-sensitive #t) (teachpacks ((lib "taxon.rkt" "installed-teachpacks"))) (htdp-settings #(#t constructor repeating-decimal #f #t none #f ((lib "taxon.rkt" "installed-teachpacks")))))
;(define (dictionary=? tree alst)
; ...)

;(define (fun t)
;  (cond
;    [(empty? t) ...]
;    [else ...(node-key t)..
;          ...(node-val t)..
;          ...(fun (node-left t))...
;          ...(fun (node-right t))..]))