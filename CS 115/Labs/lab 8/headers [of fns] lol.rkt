;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-beginner-reader.ss" "lang")((modname |headers [of fns] lol|) (read-case-sensitive #t) (teachpacks ((lib "potatohead.rkt" "installed-teachpacks"))) (htdp-settings #(#t constructor repeating-decimal #f #t none #f ((lib "potatohead.rkt" "installed-teachpacks")))))
;; Lab 8, Question 2

(define (repeat n symb)
  ...)

;; Lab 8, Question 3

(define (exponent x n)
  ...)

;; Lab 8, Question 4

(define (exponent-without-mult x n)
  ...)

;; Lab 8, Question 5

(define (largest-prime bottom top)
  ...)

;; Lab 8, Question 6

(define (make-sqr-al n b)
  ...)