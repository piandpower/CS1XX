;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-intermediate-lambda-reader.ss" "lang")((modname a4interface) (read-case-sensitive #t) (teachpacks ((lib "compound.rkt" "installed-teachpacks"))) (htdp-settings #(#t constructor repeating-decimal #f #t none #f ((lib "compound.rkt" "installed-teachpacks")))))
;; Question 1:
(define (make-grid alon)
  ...)

;; Question 2: 
(define (rle-encode dna)
  ...)

;; Question 3:
(define (rle-decode rle)
  ...)

;; Question 4:
(define (silly-string? s)
  ...)

;; Question 5
(define (num-comps alon)
  ...)
