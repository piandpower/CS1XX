;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-intermediate-lambda-reader.ss" "lang")((modname a3interface) (read-case-sensitive #t) (teachpacks ((lib "compound.rkt" "installed-teachpacks"))) (htdp-settings #(#t constructor repeating-decimal #f #t none #f ((lib "compound.rkt" "installed-teachpacks")))))
;;  interface file for A3

;; Question 1:
(define (area-under f x1 x2)
  ...)

;; Question 2: 
(define (laputa-volume t)
  ...)

;; Question 3:
(define (largest-range bars)
  ...)

;; Question 4:
(define (longest-idle-period one-computer-usage)
  ...)




